ใช้ import และเรียกใช้ฟังก์ชั่นได้เลย


import useUserLocation from "../../containers/userLocation/getUserLocation";

  // get userlocation ===================================================
  const location = useUserLocation();